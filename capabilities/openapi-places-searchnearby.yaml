naftiko: 1.0.0-alpha2
info:
  label: Google Places API (New) — places:searchNearby
  description: 'Google Places API (New) — places:searchNearby. 1 operations. Lead operation: Google Places Nearby Search.
    Self-contained Naftiko capability covering one Google Places business surface.'
  tags:
  - Google Places
  - places:searchNearby
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GOOGLE_PLACES_API_KEY: GOOGLE_PLACES_API_KEY
capability:
  consumes:
  - type: http
    namespace: openapi-places-searchnearby
    baseUri: https://places.googleapis.com/v1
    description: Google Places API (New) — places:searchNearby business capability. Self-contained, no shared references.
    resources:
    - name: places:searchNearby
      path: /places:searchNearby
      operations:
      - name: searchnearbyplaces
        method: POST
        description: Google Places Nearby Search
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: apikey
      key: key
      value: '{{env.GOOGLE_PLACES_API_KEY}}'
      placement: query
  exposes:
  - type: rest
    namespace: openapi-places-searchnearby-rest
    port: 8080
    description: REST adapter for Google Places API (New) — places:searchNearby. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/places-searchnearby
      name: places-searchnearby
      description: REST surface for places:searchNearby.
      operations:
      - method: POST
        name: searchnearbyplaces
        description: Google Places Nearby Search
        call: openapi-places-searchnearby.searchnearbyplaces
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: openapi-places-searchnearby-mcp
    port: 9090
    transport: http
    description: MCP adapter for Google Places API (New) — places:searchNearby. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: google-places-nearby-search
      description: Google Places Nearby Search
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: openapi-places-searchnearby.searchnearbyplaces
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
